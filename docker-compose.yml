version: "3.8"

services:
  openacm:
    build: .
    container_name: openacm
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./config:/app/config
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
